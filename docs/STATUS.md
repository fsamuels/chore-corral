# Chore Corral — Status

Current state of the project. Update this as work progresses — it should always reflect reality, not intent.

**Last updated:** 2026-07-03 (Tasks reorganized into full pages; activity log gets an in-app UI)

**Tasks reorganized into full pages; activity log gets an in-app UI (2026-07-03).** The `/tasks` page's create/edit `v-dialog`s are gone, replaced with dedicated routes: `/tasks/new` (create), `/tasks/:id` (a new read-only view page), and `/tasks/:id/edit` (edit) — this app's first dynamic route. `/tasks` itself is now list-only, with rows as tap/click-anywhere targets to the view page (the status `v-select` cell still works standalone via `@click.stop`); the `/map` and `/` deep-links that used to open `/tasks?task=<id>`'s edit dialog now go straight to the view page. The view page shows a task's full detail in a more readable layout, a quick status control (no need to open Edit just to change status), and — reversing the original MVP call to leave the activity log Supabase-dashboard-only (see DECISIONS.md) — that task's `activity_log` history as a timeline, most-recent-first, attributed to the member who performed it. Attribution needed a new migration (`supabase/migrations/20260704053621_activity_log_actor_profiles.sql`): a `security_invoker` view, `farm_member_profiles`, joining `farm_memberships` to `auth.users` to expose just enough (id, email) for the app to resolve an `actor_user_id` to an email — `auth.users` still isn't exposed directly. Reads go through a new `app/services/activity.ts` (`listActivityForTask`). Task creation also gained optional photos: a new `StagedTaskPhotos.vue` component holds picked photos as in-memory Files with local object-URL previews (no upload yet, since a photo's storage path needs a real task id), and `/tasks/new`'s submit flow creates the task first, then uploads each staged photo sequentially via the existing `uploadTaskPhoto`/`compressImage` pipeline — a failed photo upload doesn't undo the task creation, just surfaces a one-time warning on the resulting view page. `useTasks().create` now returns the created task (was `void`) so the create page knows the new id, and a new `getTask`/`useTask` fetch a single task by id directly (rather than loading the full farm list and finding by id), so `/tasks/:id` works correctly as a direct/bookmarked/refreshed URL and has a clean "not found" state for a bad or deleted id. 8 new Vitest cases cover `getTask` and `listActivityForTask`; `nuxt typecheck`, `eslint`, and `pnpm build` are clean. End-to-end verification (viewing a real task's activity log with attribution, staged-photo upload) still needs a real logged-in browser session — this app's invite-only Google OAuth can't be exercised from a sandboxed environment, the same limitation noted below for M4–M8.

**UI improvements (2026-07-03).** The app now has a per-user theme system: four equipment-brand Vuetify themes (Classic blue, John Deere green, Kubota orange, Massey Ferguson red), each with light and dark variants, defined in `nuxt.config.ts`. The preference is stored in Supabase user metadata (follows the user across devices) with a cookie copy so the theme applies during SSR without a flash (`app/composables/useThemePreference.ts`). Theme brand selection and dark/light mode are hosted on the new `/settings` page (previously in an app-bar account menu component that is now deleted). Alongside theming: consistent mdi icons for task status/priority/overdue across `/tasks`, `/` and `/categories` (`STATUS_DISPLAY`/`OVERDUE_ICON` added to `app/utils/task-display.ts`), the home screen's summary chips upgraded to themed stat cards (outstanding/urgent/overdue), and friendly centered empty states (icon + message + create button where relevant) replacing the plain-text empty cards on the dashboard, tasks, and categories pages.

**Components demo re-linked (2026-07-03).** The `/components-demo` Vuetify sampler (see M1 note below) had no in-app link after the M5 home-screen rewrite dropped the placeholder card that used to point to it. A new `AppFooter.vue` component — a plain "UI components demo" link to `/components-demo` — now renders in both `app/layouts/default.vue` and `app/layouts/blank.vue`, so it's reachable from every page again, authenticated or not.

## Current Phase

**Hosted setup mostly complete; M4–M8 hosted verification still outstanding.** The M2 migration is applied to the hosted project, both farms are seeded with a membership each, Google OAuth is configured and verified end-to-end (real login → farm-scoped home → farm switcher), Vercel's Preview env vars and GitHub connection are confirmed, and a Mapbox token is set (Production/Development only — Mapbox doesn't support wildcard URL token restrictions, so Preview deploys still render OSM-only until a preview-specific token or a relaxed restriction is set up). Farms have `default_lat`/`default_lng` set. Remaining: verify M4 (category create/soft-delete + `activity_log`), M5 (task lifecycle + `activity_log`), M6 (tag autocomplete/reuse) against the hosted project through the UI, and M7/M8 (GPS capture, map pins, photo capture/upload) on a real mobile device — none of which can be exercised from this sandboxed environment.

**Home screen fixed (2026-07-03).** `/` (the farm-scoped home from M3) still had its original M3 placeholder card ("Tasks will show up here once task tracking lands (M5)") even though M5 shipped the real `/tasks` page — the home screen never got wired up to show anything real. It now renders the active farm's outstanding (non-Done) tasks in the same urgent-first order as `/tasks` (`compareTasks`), with a summary row (outstanding/urgent/overdue counts), a "Manage tasks" button to the full `/tasks` CRUD page, and each list row linking to `/tasks?task=<id>` to open that task's edit dialog via the existing deep-link handling (shared with `/map`'s pin popups). The priority-chip and soft-deleted-category display logic that `/tasks` already had was extracted to `app/utils/task-display.ts` (`PRIORITY_DISPLAY`, `categoryDisplayName`) so both pages render them identically instead of duplicating the logic.

**Sample data seeded (2026-07-03).** Clarkson's Farm (the demo tenant) now carries a full themed sample data set, generated by and applied from `supabase/seeds/clarksons_farm_sample_data.sql`: 12 categories, 36 tags, 100 tasks spanning every option the schema supports (all three priorities and statuses, overdue/this-week/future/absent due dates, notes, lat/lng pins scattered around Diddly Squat's real coordinates, uncategorized tasks, 0–4 tags per task) plus 196 consistent `activity_log` entries (`category_created`/`task_created`/`task_status_changed` matching the app's `event_detail` shapes). The script is destructive-and-rerunnable — it hard-deletes all of that farm's rows and reseeds from fixed literal UUIDs, scoped so other farms are untouched — and it set the farm row's `address`/`default_lat`/`default_lng` to the real Chipping Norton location. Seeds live in `supabase/seeds/` (not `supabase/migrations/`) so sample data stays out of the schema history; apply with `supabase db query --linked --file …`. This gives the M4–M6 hosted verification passes (Next Steps 1–3) realistic data to verify against, and puts real pins on `/map` for M7.

**M8 in progress** (app-side implementation complete; storage bucket and all four per-operation RLS policies confirmed present on the hosted project via direct SQL query. End-to-end verification — actual camera capture, compression, upload, and delete — still needs a real mobile device that a sandboxed environment can't provide). Photos ride on the `task_photos` table and the private `task-photos` Storage bucket that have existed since the M2 migration — no schema change. The service layer (`app/services/photos.ts`) wraps both table and storage access: `listPhotos`, `uploadTaskPhoto` (the photo id is generated client-side since the storage path `{farm_id}/{task_id}/{photo_id}.webp` must embed it before the `task_photos` row exists; the storage upload runs before the DB insert so a failed upload never orphans a row), `deleteTaskPhoto` (same storage-then-DB ordering, in reverse), `updateTaskPhotoCaption`, and `getTaskPhotoUrl` (a short-lived signed URL, since the bucket is private) — covered by 12 new Vitest cases against a new `FakeStorageBucket` double (`tests/helpers/fake-supabase.ts`, extended with `upload`/`remove`/`createSignedUrl` and per-operation fail-injection). Client-side compression (`app/utils/photo-compression.ts`) resizes to a 1600px max edge and re-encodes to WebP via the browser Canvas API (no new npm dependency), stepping quality down from 0.82 toward a 0.5 floor until the blob is under a ~1MB target; the pure sizing math (`computeResizedDimensions`) is unit-tested, while the canvas-based encoding itself isn't (same untestable-in-happy-dom category as M7's GPS capture). No `activity_log` entries are written for photo events — photos aren't part of SPEC.md's logged-event list. The UI is a new `TaskPhotos.vue` component — camera-capture and gallery-upload buttons wired to two hidden file inputs (one with `capture="environment"`, one without), a thumbnail grid with per-photo caption editing (committed on blur) and delete, plus loading/empty/error states — backed by a `useTaskPhotos` composable that's keyed on a task id rather than the active farm (it refetches when the open task changes, not on farm switch). It's rendered only inside the `/tasks` edit dialog, not the creation dialog, since a photo's storage path requires the task to already exist.

**M7 state** (app-side implementation complete; end-to-end verification still needs a real device/GPS check that a sandboxed environment can't provide). Location rides on the `tasks.lat`/`lng` columns that have existed since the M2 migration — no schema change. The service layer carries the pin end-to-end (`TaskSummary.lat`/`lng`, create/update payloads) behind an exported `assertValidLocation` guard (both-or-neither, range-checked, `0`-safe), unit-tested with 8 new Vitest cases; location edits are unlogged field edits like notes/due date. Maps follow ARCHITECTURE.md: Leaflet + `@vue-leaflet/vue-leaflet`, with tile providers centralized in `app/utils/map-tiles.ts` — Mapbox Satellite Streets (hybrid) default with an OSM street toggle via the layer control, degrading to OSM-only when `NUXT_PUBLIC_MAPBOX_TOKEN` is unset (now set for Production/Development; Preview still falls back to OSM since Mapbox token URL restrictions don't support wildcards for per-preview-deploy URLs). Three client-only components (`.client.vue`, since Leaflet touches `window` at import): `FarmMap` (shared base map: tiles, layer control, marker-icon bundler fix, mapClick/ready events), `LocationPicker` (SPEC's capture flow — task creation auto-attempts a GPS fix shown on a mini-map for confirm/adjust by dragging the pin or tapping the map; denied/failed GPS falls back to manual placement or saving pinless; edit dialog gets the same picker without auto-capture so editing never overwrites an existing pin with the editor's position), and `TaskMap` (pins + popups). The `/map` page scopes to the active farm, fits the view to the located tasks' bounds (else the farm's `default_lat`/`default_lng`, which `useFarms` now fetches), and each pin's popup deep-links to `/tasks?task=<id>`, which opens that task's edit dialog. The Mapbox usage-tier open question in DECISIONS.md is resolved (Static Tiles free tier is ~two orders of magnitude above expected volume).

**M6 state** (app-side implementation complete; end-to-end verification still pending against the hosted project — the `/tasks` page is behind auth middleware so it can't be smoke-tested in a sandboxed environment). Tags build on the M4/M5 service-layer pattern: `app/services/tags.ts` adds `listTags` (per-farm, alphabetical), `resolveTags` (get-or-create from freeform name input — trims, drops empties, and dedupes case-insensitively in JS since Postgres `text` comparison is case-sensitive and the farm-scoped `unique(farm_id, name)` constraint needs exact-enough matching to avoid near-duplicate tags like "Fence"/"fence"), `setTaskTags` (replaces a task's full tag set: delete then bulk-insert), and `listTagsForTasks` (bulk per-task lookup via two sequential queries rather than a join, matching the fake test client's and this codebase's no-embedded-resource style). `app/services/tasks.ts` now resolves and attaches tags on `createTask`/`updateTask` and bulk-attaches them on `listTasks`, so `TaskSummary.tags` is populated end-to-end. 13 new Vitest cases in `tests/tags.test.ts` plus 4 more in `tests/tasks.test.ts` cover creation/reuse/case-insensitive dedup/farm-scoping and the create/update/list integration. The UI adds a `useTags` composable (read-only, mirrors `useCategories`) and a `v-combobox` tags field (multiple, chips, freeform autocomplete against the farm's existing tags) to both the task creation dialog's "More details" expand and the task edit dialog, plus small tag chips shown under each task's title in the `/tasks` list view.

**M5 state** (app-side implementation complete; end-to-end verification against the hosted project still pending). Task CRUD follows the M4 layering: a service layer (`app/services/tasks.ts`) wraps all Supabase access — `listTasks` (sorted urgent-first client-side with an oldest-first tiebreak via an exported `compareTasks`), `createTask`, `updateTask` (field edits only, deliberately unlogged per SPEC), `changeTaskStatus` (sets `completed_at` on done, clears it on reopen, logs the old/new pair, no-ops on a same-status call), hard `deleteTask`, and a pure `isTaskOverdue` (local-calendar-date comparison; due today ≠ overdue). Task events (`task_created` / `task_status_changed` / `task_deleted`) land in `activity_log` with the `task_title` snapshot DATA_MODEL.md requires. The lifecycle logic — an ARCHITECTURE.md priority test area — is covered by Vitest cases in `tests/tasks.test.ts` against the in-memory Supabase fake (which gained `delete()` support). The UI is a `/tasks` page: a priority-ordered `v-data-table` scoped to the active farm, category filter (All / Uncategorized / per-farm categories), inline status select per row, overdue chip + red due date, strikethrough for done tasks, a creation dialog (Title/Category/Priority with a "More details" expand for Notes and Due date), an everything-expanded edit dialog hosting the delete confirm, and a `useTasks` composable that re-fetches on farm switch, accessible from the navigation drawer. Tasks referencing a soft-deleted category render "(deleted category)" rather than silently reassigning. Exercising the full lifecycle against the hosted project (now unblocked — see Next Steps) hasn't happened yet.

**M4 state** (app-side implementation complete; end-to-end verification against the hosted project still pending). Category management lives behind a service layer (`app/services/categories.ts`) that wraps all Supabase access: `listCategories` / `createCategory` / `deleteCategory`, with soft-deletion refused while any `not_started`/`in_progress` task references the category (surfaced as a typed blocked result, not an exception) and `category_created` / `category_deleted` events written to `activity_log` with a category-name snapshot in `event_detail`. That delete guard — M4's called-out priority test area — is unit-tested with 12 Vitest cases against a seeded in-memory fake of the Supabase query chains (`tests/categories.test.ts`, `tests/helpers/fake-supabase.ts`). The UI is a `/categories` page scoped to the active farm (create form, confirm-dialog deletion, blocked-delete warning, empty/loading/error states) backed by a `useCategories` composable that re-fetches on farm switch, accessible from the navigation drawer. `@supabase/supabase-js` is now a direct devDependency so services can import the `SupabaseClient` type under pnpm's isolated layout. Creating/deleting real categories against the hosted project hasn't happened yet, though the migration and OAuth setup that were blocking it are now done.

**M3 state — done and verified end-to-end.** The app-side auth flow is implemented via `@nuxtjs/supabase`: a `/login` page with Google `signInWithOAuth`, the `/confirm` OAuth callback (with error/timeout states), and route protection through the module's auth middleware plus a global membership middleware. The unrecognized-login state is a dedicated `/no-access` page — an authenticated user whose RLS-scoped `farms` query returns zero rows is redirected there and sees the invite-only explanation, their signed-in email, and a sign-out button. Farm switching is handled on the `/farm` page via a selectable farm list, backed by a `useFarms` composable with the active-farm selection persisted in a cookie; `/` is now the authenticated home showing the active farm. Google OAuth is configured on the hosted Supabase project (client credentials in `supabase/config.toml`'s `[auth.external.google]`, pushed via `supabase config push` with `SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID`/`SECRET` env vars; Auth URL config's Site URL is the production Vercel URL with redirect allow-list entries for localhost, production, and Vercel preview `/confirm` paths), and verified live: real Google login → `/no-access` before a membership existed → farm-scoped home + working farm switcher after seeding the membership.

**M2 state — done.** The full MVP schema exists as a versioned Supabase CLI migration (`supabase/migrations/20260702154910_m2_schema_and_rls.sql`): both enums, all eight tables per DATA_MODEL.md, deny-by-default RLS with the farm-membership pattern on every farm-scoped table (append-only `activity_log`), and the private `task-photos` storage bucket with path-based per-operation policies. The migration and its RLS behavior were validated against a local Postgres 16 cluster with a shim of the Supabase-managed surface — 13 impersonation tests confirm cross-farm isolation for reads, writes, and storage paths. The migration is now applied to the hosted project (`supabase migration list` confirms local/remote in sync), both farms (Reign Cloud Ranch, Clarkson's Farm) are seeded with `default_lat`/`default_lng` set, and each has a membership row for the first real user.

Earlier M1 state: Nuxt + Vuetify app scaffolded with the full tooling set (ESLint, Prettier, vue-tsc, Husky/lint-staged, Vitest, GitHub Actions CI); `app/pages/` file-based routing with the scaffold landing card at `/` and a Vuetify component sampler at `/components-demo` (demo/reference only). Supabase project `chore-corral` (us-east-2) created with public env vars wired into the local app and Vercel (Production/Development environments).

## Milestone Progress

| Milestone                | Status      |
| ------------------------ | ----------- |
| M1 — Scaffold & Deploy   | Done        |
| M2 — Schema & Data Layer | Done        |
| M3 — Auth                | Done        |
| M4 — Category Management | In Progress |
| M5 — Core Task CRUD      | In Progress |
| M6 — Tags & Autocomplete | In Progress |
| M7 — Location & Map View | In Progress |
| M8 — Photos              | In Progress |
| M9 — Polish & Hardening  | Not started |

## Known Issues

- **Fixed:** `useCategories.ts`/`useTasks.ts` derived the acting user's id via `user.value?.id`, but `useSupabaseUser()` returns the decoded access-token JWT payload, whose id claim is `sub` — `id` doesn't exist on that payload (TypeScript didn't catch it because `JwtPayload` has a permissive `[key: string]: any` index signature). This made `actorUserId` `undefined` for every signed-in user, every time: category creation silently no-op'd (the guard just `return`s) while task create/update/status-change/delete threw `"No active farm or signed-in user"`. This is very likely why M4/M5 hosted verification (see Next Steps) kept getting deferred — the UI would have failed immediately. Fixed by extracting a `getActorUserId` helper (`app/utils/auth.ts`) that reads `.sub`, covered by `tests/auth.test.ts`; both composables now use it.
- Vercel's PR preview build for the M3 branch initially failed with `[vite]: Rollup failed to resolve import "tslib" from ".../@supabase/functions-js/.../FunctionsClient.js"`, even though `pnpm build` passed both locally and in GitHub Actions CI on the same commit. It reproduced across multiple pushes (including one that added `tslib` as an explicit direct dependency, which didn't help) and was resolved by clearing Vercel's build cache and redeploying — pointing to a stale/corrupted cached `node_modules` on Vercel's side rather than a real dependency-resolution bug. The same class of issue showed up locally too: after a `supabase` CLI Homebrew upgrade, `pnpm dev` threw `Cannot find module 'tslib'` even though it's a locked dependency — `rm -rf node_modules && pnpm install` fixed it. Worth remembering if a build/dev-server fails on a dependency error that a fresh install elsewhere doesn't reproduce: try a clean reinstall (or cache-cleared redeploy on Vercel) before chasing a code fix.
- Mapbox access tokens don't support wildcard URL restrictions, so the token set for Preview would need to be unrestricted or per-preview-URL — for now `NUXT_PUBLIC_MAPBOX_TOKEN` is only set for Production and Development, and Preview deploys render OSM-only street tiles (the existing no-token fallback), which is harmless but worth knowing if a preview build's map looks different from prod's.

## Next Steps

1. Verify M4's done-state against the hosted project: create and soft-delete a category on each farm and confirm both events land in `activity_log` (the active-task guard is already unit-tested; M5's task creation now makes it exercisable for real)
2. Verify M5's done-state against the hosted project: run a full task lifecycle — create, edit, change status (checking `completed_at` sets on done and clears on reopen), delete — on `/tasks` under RLS, and confirm `task_created`/`task_status_changed`/`task_deleted` entries land in `activity_log`
3. Verify M6's done-state against the hosted project: add tags to a task at creation and at edit time, confirm autocomplete suggests existing per-farm tags (including reusing a tag typed with different casing rather than creating a near-duplicate), and confirm tags persist correctly across a farm switch
4. Verify M7's done-state on a real mobile device against the hosted project: create a task from the field with GPS auto-capture (confirm the mini-map's confirm/adjust flow and the permission-denied manual fallback), see the pin correctly placed on `/map` with the satellite/street layer toggle working, and tap through a pin's popup to the task's edit dialog
5. Verify M8's done-state on a real mobile device against the hosted project: take a photo with the camera-capture button and upload one via the gallery button from a task's edit dialog, confirm both land compressed within the ~500KB–1MB target, display correctly with editable captions, and confirm deleting a photo removes both the storage object and its `task_photos` row
6. Optional: work out a Mapbox token strategy for Preview deploys (e.g. an unrestricted preview-only token) if satellite tiles are wanted in PR previews
7. Verify the reorganized tasks pages on a real logged-in session: create a task with a staged photo, confirm it uploads correctly once the task exists; open a task's view page and confirm its activity log renders with correct attribution (email) for each event; edit and delete from the new full pages; confirm a bad/deleted task id at `/tasks/:id` shows the not-found state instead of erroring

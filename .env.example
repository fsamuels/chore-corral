# Supabase — client-exposed public values (see docs/ARCHITECTURE.md).
# Copy to .env and fill in from the Supabase dashboard: Project Settings → API.
NUXT_PUBLIC_SUPABASE_URL=
NUXT_PUBLIC_SUPABASE_KEY=

# Mapbox — public access token for satellite map tiles (see docs/ARCHITECTURE.md).
# Optional: without it the map view falls back to OSM street tiles only.
NUXT_PUBLIC_MAPBOX_TOKEN=

# Database password (not used by the app — for direct DB access / supabase CLI).
# SUPABASE_DB_PASSWORD=

# Google OAuth client (Supabase auth.external.google) — from Google Cloud Console
# Credentials, used by `supabase config push` to configure the hosted project's
# Google provider. Not read by the Nuxt app itself.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# Playwright e2e tests only (tests/e2e/global-setup.ts) — never used by the
# app itself. This key bypasses RLS to seed a test user/farm; it must never
# be exposed to the client or committed. Prefer a Secret API key (Supabase
# dashboard → Project Settings → API Keys → Create new secret key) over the
# legacy service_role key — same RLS-bypass capability, but independently
# revocable if it leaks, rather than tied to the project's JWT secret (whose
# rotation invalidates every user's session). See DECISIONS.md.
# Leave the three optional vars below commented/absent rather than set to an
# empty string — global-setup.ts's `??` fallback only applies when a var is
# genuinely unset, not when it's present-but-empty (see DECISIONS.md).
# SUPABASE_SERVICE_ROLE_KEY=
# E2E_TEST_USER_EMAIL=
# E2E_TEST_USER_PASSWORD=
# E2E_TEST_FARM_NAME=
